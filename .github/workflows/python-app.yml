name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.VPS_HOST }}
        username: ${{ secrets.VPS_USER }}
        key: ${{ secrets.VPS_SSH_KEY }}
        script: |
          cd /opt/trackimmo
          systemctl stop trackimmo-api
          git pull origin main
          source venv/bin/activate
          pip install prometheus_client || echo "prometheus_client install failed"
          pip install -r requirements.txt || pip install fastapi uvicorn supabase pydantic-settings sqlalchemy python-dotenv
          systemctl start trackimmo-api
          sleep 10
          curl -f http://localhost:8000/health || exit 1
